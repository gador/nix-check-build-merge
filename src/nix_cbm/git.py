import logging


# TODO: add function
def git_checkout(commit: str):
    """ "
    checkout the local nixpkgs to the specified commit
    """
    logging.info(f"Will now checkout commit ${commit}")
    return True
